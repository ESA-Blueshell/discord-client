# Kept in step with typescript/package.json by release-please; see
# release-please-config.json. Both artefacts share the repository's version
# because they are generated from one spec surface and released together.
version=0.2.4 # x-release-please-version
group=net.blueshell.clients

org.gradle.caching=true
org.gradle.parallel=true
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
